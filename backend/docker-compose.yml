version: '3.8'

services:
  face-processor:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
      - INSIGHTFACE_ROOT=/app/backend/models
    volumes:
      - ./models:/app/backend/models
    restart: unless-stopped
